// Tailwind CSS v4 PostCSS plugin — required for `@import "tailwindcss"` to
// generate utility classes under Next.js / Turbopack.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
}

export default config
